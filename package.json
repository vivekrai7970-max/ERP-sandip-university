{
  "name": "erp-sandip-university",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "python -m pip install -r requirements.txt && python sandip_university/manage.py migrate --noinput && python sandip_university/manage.py collectstatic --noinput"
  }
}
